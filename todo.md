передача функций в теле верстки считается плохой практикой - лучше вынести ее в переменную
<Button className="" variant="contained" onClick={() => {

список чатов тоже нужно вынести в компонент, который будет вызываться роутером
иначе у вас вылезет выбор чата на странице профиля

BrowserRouter нужно унести в файл, где происходит вызов ReactDOM.render
говорила об этом на уроке
